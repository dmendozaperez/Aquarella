@using informativa.aquarella.com.oe.Models
@model IEnumerable<Ent_PasarelaDetalle>
@{
    ViewBag.Title = "Pagina Principal";
}
@*<link href="@Url.Content("~/Content/bootstrap-select.min.css")" rel="stylesheet" type="text/css" />*@
<script src="@Url.Content("~/assets/bootstrap/js/bootstrap.min.js")"></script>


<div class="slider-container">
    <div class="container">
        <div class="row">
            <div class="col-sm-10 col-sm-offset-1 slider">
                <div class="flexslider">
                    <ul class="slides">

                        @foreach (Ent_PasarelaDetalle item in Model)
                        {
                            <li data-thumb="../@item.PasarelaDet_Ruta@item.PasarelaDet_Nombre">
                                <img src="~/@item.PasarelaDet_Ruta@item.PasarelaDet_Nombre">
                            </li>
                        }

                                                              
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
<script src="~/assets/js/jquery-1.11.1.min.js"></script>
<script src="~/assets/bootstrap/js/bootstrap.min.js"></script>
<script src="@Url.Content("~/assets/bootstrap/js/bootstrap.min.js")"></script>
<script>
    $(document).ready(function () {
       
        $(".btnconsultar").click(function () {
         

            var TeamDetailPostBackURL ="@Url.Action("Editar", "Pasarela")";
            var $buttonClicked = $(this);
            var id = $buttonClicked.attr('data-id');

            var options = { "backdrop": "static", keyboard: true };
            $.ajax({
                type: "GET",
                url: TeamDetailPostBackURL,
                contentType: "application/json; charset=utf-8",
                data: { "strId": id },
                datatype: "json",
                cache: true,
                success: function (data) {
                    //waitingDialog.hide();

                    ////debugger;
                    //$('#myModalContent').html(data);
                    //$('#myModal').modal(options);
                    //$('#myModal').modal('show');

                },
                error: function () {
                    //waitingDialog.hide();
                    // waitingDialog.hide();
                    //removeLoading();
                    alert("Dynamic content load failed.");

                }
            });
        });

    });

   

</script>